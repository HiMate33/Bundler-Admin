export default function Dashboard() {
    return (
        <div>
            <h1 className="bg-red">Welcome to the dashboard</h1>
        </div>
    );
}
// This component is used in the landing page to display the dashboard section.